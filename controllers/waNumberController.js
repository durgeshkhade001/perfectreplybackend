const WaNumber = require("../models/waNumber");
const { authenticateAgent } = require('../utils/authenticateAgent');
const axios = require('axios');

const create_new_wa_number = async (req, res) => {
}

module.exports = {
    create_new_wa_number,
}